describe("Example test", () => {
  test("should behave correctly", () => {
    // GIVEN
    const value = 2;

    // WHEN
    const result = value * 2;

    // THEN
    expect(result).toBe(4);
  });
});